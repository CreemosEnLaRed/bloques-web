Blockly.JavaScript['hs_par'] = function(block) {
  var statements_items = Blockly.JavaScript.statementToCode(block, 'ITEMS');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['hs_title'] = function(block) {
  var dropdown_level = block.getFieldValue('LEVEL');
  var statements_items = Blockly.JavaScript.statementToCode(block, 'ITEMS');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['hs_text'] = function(block) {
  var text_text = block.getFieldValue('TEXT');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['hs_list'] = function(block) {
  var dropdown_type = block.getFieldValue('TYPE');
  var statements_items = Blockly.JavaScript.statementToCode(block, 'ITEMS');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['hs_li'] = function(block) {
  var statements_items = Blockly.JavaScript.statementToCode(block, 'ITEMS');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['hs_div'] = function(block) {
  var statements_items = Blockly.JavaScript.statementToCode(block, 'ITEMS');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['hs_wraptext'] = function(block) {
  var text_text = block.getFieldValue('TEXT');
  var dropdown_type = block.getFieldValue('TYPE');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['hs_img'] = function(block) {
  var text_src = block.getFieldValue('SRC');
  var text_alt = block.getFieldValue('ALT');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['hs_link'] = function(block) {
  var text_href = block.getFieldValue('HREF');
  var text_label = block.getFieldValue('LABEL');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};